# Local benchmarks

Generated measurements and workload-specific harnesses do not belong to the
TradeEngine source repository. Keep them in the private strategy or release
evidence repository that owns the workload.

TradeEngine tests may verify generic timing, lifecycle and evidence contracts,
but this directory must not contain a strategy identity, private Dataset,
canonical request, expected Result, or strategy-specific acceptance threshold.
